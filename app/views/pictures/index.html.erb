<h1>Pictures#index</h1>
<p>Find me in app/views/pictures/index.html.erb</p>

<% @pictures.each do |picture| %>
<div>
  <p><%= picture.title %> by: <%= picture.artist %></p>
  <%=link_to(image_tag("#{picture.url}", size: "500x300"), picture_path(picture.id)) %>
</div>
<% end %>
